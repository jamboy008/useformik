import React from "react";
const variant = {
    delete: "bg-red-600 text-white p-2"
};

function Button({ text, variant }) {
  return <button className={`${}`}>{text}</button>;
}

export default Button;
